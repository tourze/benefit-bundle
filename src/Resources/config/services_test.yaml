services:
  _defaults:
    autowire: true
    autoconfigure: true

  # DataFixtures should only be available in test environment
  BenefitBundle\DataFixtures\BenefitFixtures:
    tags: ['doctrine.fixture.orm']